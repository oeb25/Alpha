# Alpha
Tests and what not, opening stages

## Building and development

### Requirements

- `Node.js`

Run the following commands to start development server
and build.

```
npm install
npm start
```

Now go to [`localhost:3000`](localhost:3000) in your browser
